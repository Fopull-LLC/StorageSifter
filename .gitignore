/target
**/*.rs.bk

# Local editor / agent tooling state
/.claude/

# Cargo.lock is intentionally committed: StorageSifter ships binaries and we
# pin exact dependency versions for reproducible builds.
