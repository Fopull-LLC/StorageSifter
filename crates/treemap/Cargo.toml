[package]
name = "treemap"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Squarified treemap layout for StorageSifter (Phase 2)."

[dependencies]
